{
  "name": "future-blue-shark-player",
  "version": "4.8.0",
  "private": true,
  "scripts": {
    "start": "npm run db:migrate && node api/server.js",
    "db:migrate": "node db/migrate.js",
    "test": "node --test tests/*.test.js",
    "test:staging": "node scripts/test-staging.js",
    "security:check": "node scripts/security-check.js",
    "release:check": "npm test && npm run security:check && npm run prod:check && npm run android:contract",
    "load:test": "node scripts/load-test.js",
    "prod:check": "node scripts/production-check.js",
    "android:contract": "node scripts/android-contract-check.js",
    "render:start": "npm run db:migrate && node api/server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "zod": "^4.1.5"
  }
}
